import styled from "styled-components"

export const DefaultTagContainer = styled.div`
    display: inline-flex;
    flex-direction: column;
    flex: 0 1 auto;
    overflow: hidden;
    background-color: rgba(245, 245, 245, 0.6);
    padding: 10px;
    border-radius: 10px;
`

export const DefaultTagRow = styled.div`
    display: flex;
    flex-direction: row;
    flex: 1;
`
