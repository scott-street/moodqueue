import styled from "styled-components"
import { Box as GrommetBox, RangeInput as NumberSelector } from "grommet"

export const OuterBox = styled(GrommetBox)`
    flex-direction: row;
    display: flex;
    flex: 1;
    align-items: center;
`

export const SizePicker = styled(NumberSelector)``
