import styled from "styled-components"
import { Box as GrommetBox } from "grommet"

export const MoodBox = styled(GrommetBox)`
    border-radius: 30px 30px 0px 30px;
    outline: none;
    display: flex;
    flex: 1;
    align-items: center;
    text-align: center;
`
